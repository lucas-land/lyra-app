import { Injectable } from '@angular/core';

/**
 * Serviço para integração com Firebase.
 * Estrutura preparada para futura implementação (Auth, Firestore, Storage, etc.).
 */
@Injectable({
  providedIn: 'root',
})
export class FirebaseService {
  constructor() {
    // TODO: inicializar Firebase usando environment (quando configurado)
  }

  // TODO: métodos de autenticação
  // TODO: métodos de Firestore (notícias, eventos)
  // TODO: métodos de Storage (upload de imagens)
}
